@using Microsoft.Ajax.Utilities
@model List<Domain.Invoice>

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">@Resources.Manage.Invoices_Header</h3>
            </div><!-- /.box-header -->
            @Html.ActionLink(Resources.Manage.Invoices_Add, "Create", new { }, new { @class = "btn btn-block btn-primary btn-flat" })
            <div class="box-body">
                <table id="example2" class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>SUM</th>
                            <th>Payment Method</th>
                            <th>Coupon</th>
                            <th>Phone</th>
                            <th>Username</th>
                            <th>Done</th>
                            <th>Canceled</th>
                            <th>@Resources.Common.Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.CreationTime</td>
                                <td>@item.SumAfterCoupon</td>
                                <td>
                                    @{
                                        if (item.PaymentMethodId != null)
                                        {
                                            @item.PaymentMethod.PaymentMethodName.Value
                                        }
                                    }
                                </td>
                                <td>
                                    @{
                                        if (item.CouponId != null)
                                        {
                                            @item.Coupon.Code
                                        }
                                    }
                                </td>
                                <td>@item.CustomersPhone</td>
                                <td>
                                    @{
                                        if (item.UserIntId != null)
                                        {
                                            @item.UserInt.UserName
                                        }
                                    }
                                </td>
                                <td></td>
                                <td></td>
                                <td>@Html.ActionLink(Resources.Common.Edit, "Edit", new { id = item.InvoiceId }) | @Html.ActionLink(Resources.Common.Delete, "Delete", new { id = item.InvoiceId })</td>
                            </tr>
                        }
                    <tfoot>
                        <tr>
                            <th>Date</th>
                            <th>SUM</th>
                            <th>Payment Method</th>
                            <th>Coupon</th>
                            <th>Phone</th>
                            <th>Username</th>
                            <th>Done</th>
                            <th>Canceled</th>
                            <th>@Resources.Common.Actions</th>
                        </tr>
                    </tfoot>
                </table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
</div>
